import type { SkillConfig } from "@/types/skill";

export const skillConfig: SkillConfig = {
  id: "pool_training_shared",
  name: "Pool Training Shared",
  description: "Shared pool training tools and helpers.",
  icon: "Target",
  version: "1.0.0",
  author: "cc-web",
  tags: ["pool", "training"],
};
